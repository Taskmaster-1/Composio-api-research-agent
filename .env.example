# copy to .env and fill in — any subset of the three LLM keys works;
# the client falls back down the chain (Groq -> OpenRouter -> Gemini)
GROQ_API_KEY=
OPENROUTER_API_KEY=
GEMINI_API_KEY=

# google search via serper.dev (free tier is plenty)
SERPER_API_KEY=
